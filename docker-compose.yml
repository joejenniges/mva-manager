services:
  osrm:
    image: osrm/osrm-backend:latest
    container_name: mva-osrm
    ports:
      - "5000:5000"
    volumes:
      - osrm_data:/data
    command: osrm-routed --algorithm mld /data/region.osrm
    restart: unless-stopped

volumes:
  osrm_data:
